blueprint:
  name: Voice - AI Image Generator
  author: TriTue2011 nguồn luuquangvu
  description: |
    # Công cụ tạo ảnh bằng AI từ mô tả văn bản cho Voice Assistant
    ## Hướng dẫn cài đặt
    ### Bắt buộc
    * Phải tạo và cấu hình AI Task entity hỗ trợ tạo ảnh trong phần Hệ thống - Cài đặt chung.
    ### Tùy chọn
    * Điều chỉnh các prompt cho từng trường trong script. Mô tả sẽ hướng dẫn LLM cung cấp đầu vào đúng.
    * Cấu hình AI Task entity tùy chỉnh nếu muốn dùng model cụ thể.
    ### Lưu ý
    * Cung cấp mô tả ngắn gọn và chính xác cho script để LLM nhận biết đây là script tạo ảnh từ prompt văn bản.
    * Nhớ expose script cho Assist sau khi lưu.
    * Không thay đổi tên script mặc định.
  domain: script
  homeassistant:
    min_version: 2025.8.0

  input:
    ai_task_settings:
      name: Settings for AI Task
      icon: mdi:robot-outline
      description: These settings allow you to set up the AI Task responsible for handling the image generation task.
      collapsed: true
      input:
        ai_task_entity:
          name: AI Task Entity
          description: If left empty, the system will use the default settings under System - General. Make sure to select an AI Task that supports image generation.
          selector:
            entity:
              filter:
              - domain:
                - ai_task
              multiple: false
          default:

    output_settings:
      name: Settings for Image Output
      icon: mdi:folder-image
      description: Configure where generated images should be stored and how filenames are generated.
      collapsed: true
      input:
        output_directory:
          name: Output Directory
          description: Absolute path where the generated image will be stored (default is `/media`).
          selector:
            text:
              multiple: false
              multiline: false
          default: /media
        filename_prefix:
          name: Filename Prefix
          description: Prefix used when generating the image filename.
          selector:
            text:
              multiple: false
              multiline: false
          default: ai_generated_
        file_extension:
          name: File Extension
          description: File extension to use for the generated image (for example jpg or png).
          selector:
            text:
              multiple: false
              multiline: false
          default: png

    prompt_settings:
      name: Prompt settings for the LLM
      icon: mdi:robot
      description: You can use these settings to finetune the prompts for your specific LLM (model). In most cases the defaults should be fine.
      collapsed: true
      input:
        instructions_prompt:
          name: Instructions Prompt
          description: The prompt used for the LLM to describe the image generation request in detail.
          selector:
            text:
              multiline: true
              multiple: false
          default: |
            This argument is mandatory and must always be provided.
            Describe in rich detail what image you want to generate. Focus on realism, natural lighting, and ultra-detailed visual quality.
            The image should look like a real high-resolution photo (8K), realistic, ultra-detailed, and naturally lit.
            Do not include any text, watermark, logo, or hashtags. Do not add AI marks.
            Always aim for photo-realistic, cinematic, and lifelike render quality.
            Use descriptive elements like environment, perspective, lighting, and texture.
            If you describe a person, describe gender, age, clothes, pose, camera angle, and background.
            Examples:
            - "A high-resolution (8K) photo of a golden retriever puppy lying on a wooden floor with sunlight through a window, realistic, ultra-detailed, natural lighting, no watermark, no text."
            - "A realistic portrait photo of a Vietnamese woman wearing an Ao Dai in natural daylight, ultra-detailed face and skin, high-quality photo, no watermark, no logo."
            - "A detailed photo of a futuristic city street at night, cinematic lighting, ultra-realistic reflections, 8K resolution, no watermark or text."

        task_name_prompt:
          name: Task Name Prompt
          description: The prompt for the LLM to provide a short and descriptive task name for the image generation.
          selector:
            text:
              multiline: true
              multiple: false
          default: |
            This argument is optional.
            Provide a short, natural, and descriptive task name for the image generation request.
            The name should reflect the subject or style of the image (for example "8K Realistic Portrait", "Ultra-detailed Street Scene", "Natural Lighting Landscape").
            Avoid adding any hashtags, symbols, or AI-related terms.
            If not provided, the name will be auto-generated based on the instructions.

  source_url: https://github.com/TriTue2011/Blueprint/blob/main/ai_image_generator_full_llm.yaml

mode: parallel
max: 10
max_exceeded: silent

variables:
  version: 20251029

fields:
  instructions:
    name: Instructions
    description: !input instructions_prompt
    selector:
      text:
        multiline: true
    required: true
  task_name:
    name: Task Name
    description: !input task_name_prompt
    selector:
      text:
    required: false

sequence:
  - variables:
      ai_task_entity: !input ai_task_entity
      instructions: '{{ instructions | default("") | trim }}'
      task_name_input: '{{ task_name | default("") | trim }}'

  - alias: Validate instructions
    if:
      - condition: template
        value_template: '{{ not instructions }}'
    then:
      - alias: Set variable for error message
        variables:
          response:
            error: Unable to generate image because instructions are missing or empty.
      - alias: Stop the script
        stop: Unable to generate image because instructions are missing or empty.
        response_variable: response

  - variables:
      auto_task_name: |
        {% if task_name_input %}
          {{ task_name_input }}
        {% else %}
          {% set words = instructions.split()[:5] %}
          {{ words | join(' ') | truncate(50, True, '') }}
        {% endif %}

  - alias: Generate image using AI Task
    if:
      - condition: template
        value_template: '{{ not ai_task_entity }}'
    then:
      - action: ai_task.generate_image
        data:
          task_name: '{{ auto_task_name }}'
          instructions: '{{ instructions }}'
        response_variable: ai_result
    else:
      - action: ai_task.generate_image
        data:
          task_name: '{{ auto_task_name }}'
          instructions: '{{ instructions }}'
          entity_id: '{{ ai_task_entity }}'
        response_variable: ai_result

  - alias: Check if image generation was successful
    if:
      - condition: template
        value_template: '{{ ai_result is defined and ai_result.media_source_id is defined }}'
    then:
      - variables:
          image_path_from_media_source: |
            {% set source_id = ai_result.media_source_id %}
            {% if source_id.startswith('media-source://ai_task/image/') %}
              {{ '/media/ai_task/image/' ~ source_id.split('/')[-1] }}
            {% else %}
              {{ source_id }}
            {% endif %}
          response:
            image_path: '{{ image_path_from_media_source }}'
            task_name: '{{ auto_task_name }}'
            instructions: '{{ instructions }}'
            status: Image generated successfully
            media_source_id: '{{ ai_result.media_source_id }}'
            url: '{{ ai_result.url | default("") }}'
            model: '{{ ai_result.model | default("") }}'
            revised_prompt: '{{ ai_result.revised_prompt | default("") }}'
    else:
      - alias: Set error response
        variables:
          response:
            error: Image generation failed. The AI Task did not return a valid image URL.
      - alias: Stop script with error
        stop: Image generation failed. The AI Task did not return a valid image URL.
        response_variable: response

  - stop: ''
    response_variable: response
